#pragma once

#include <string>
#include <map>

class Importer {
    public:
        Importer();
        Importer(std::string path);
        //std::map<std::string, 

    private:
        std::string filePath;
        //std::map
};

