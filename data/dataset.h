#pragma once

#include <string>
#include <map>

class Dataset {
    public:
        Dataset();
        Dataset(std::string path);
        //std::map<std::string, 

    private:
        std::string filePath;
        //std::map
};

